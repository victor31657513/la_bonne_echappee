# Repository Guidelines

- Run `npx eslint .` after modifying JavaScript code. Failures should be fixed before committing.
- If a `package.json` with a `test` script is present, run `npm test` after code changes.
- Documentation-only changes do not require linting or tests.
- Source files are located in `src/`.
